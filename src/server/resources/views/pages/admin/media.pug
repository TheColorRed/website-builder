extends main

block content
  script.
    const TRASH_URL = '#{route.name("api-admin-delete-media")}'
    const FILTER_URL = '#{route.name("api-admin-filter-media")}'
    const FILES_URL = '#{route.name("api-admin-media-files")}'
    const FILE_URL = '#{route.name("api-admin-media-file")}'
  block head
  block media